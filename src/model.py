class Model:

    def predict(self, x_test: np.ndarray) -> np.ndarray:
        """
        This method should handle making predictions with a trained model.

        :param x_test: 2-dimensional np.array of shape (n, f) containing n elements
        with f features.
        :return: 1-dimensional np.array of the same length as x_test containing
        predictions for each point in x_test.
        """
        pass